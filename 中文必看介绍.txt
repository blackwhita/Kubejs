id可有可无，如果没有id，kjs会默认提供一个id，
item是物品id
fluid是流体id，你可以获取流体桶的id，然后删去桶的id
block是方块id，也算物品的一部分，但是必须是可放置的方块，
tag是矿物词典（旧版），例如'forge:ingots/iron'
fluidTag是流体tag,
value是数字，可以是小数也可以是整数，视情况而定,
类型配方中第一种是用于新手，批量配方，已经做好的，//第二种为//后面的，是为会的提供源配方改写用的，//第三种！！！！！后面的是添加附属后的配方
input默认为输入，output默认为输出
